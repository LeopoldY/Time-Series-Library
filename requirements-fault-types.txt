# Activate the server's existing CUDA PyTorch environment first.
# Install torch with the CUDA build appropriate for that server separately.
torch>=2.2
numpy>=1.23,<2
pandas>=1.3
scikit-learn>=1.0
einops==0.8.0
reformer-pytorch==1.4.4
local-attention==1.9.14
pytest>=7
