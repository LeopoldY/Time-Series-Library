# 两阶段适配验证记录

日期：2026-09-14。本次未启动真实训练，未调用训练循环或optimizer.step，未生成训练输出目录。

- 原有二分类测试与新增两阶段测试共6项通过。
- 三种主干均通过回归梯度检查：梯度到达主干、未使用分类头无梯度。
- 三种主干均通过冻结检查：分类梯度到达头，主干无梯度，重复调用train模式后主干仍处于eval，其全部参数及buffer校验值不变。
- 人工序列确认回归目标为下一小时的四个原始计数；含末尾小批次的整体MSE=3.5、MAE=1.5与手算一致。
- 回归checkpoint重载和参数校验通过；不同数据SHA256、历史长度或错误状态校验值均拒绝加载。
- 全模型Shell脚本默认预检查完成48组（3模型×4设备×4长度）；每组均未进入训练分支，统计见preflight.json。
- Shell及Python语法检查通过；结果汇总工具通过人工结果文件验证。

仅在本地CPU及现有临时PyTorch环境验证，未验证CUDA训练、完整训练收敛或性能。旧联合训练入口保留；新流程使用run_safe_two_stage.py。
